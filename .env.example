# --- Atlassian 配置 ---
ATLASSIAN_URL="https://your-domain.atlassian.net"
ATLASSIAN_EMAIL="your-email@example.com"
ATLASSIAN_API_TOKEN="粘贴你从Atlassian官网获取的API Token"

# --- Jira 配置 ---
JIRA_PROJECT_KEY="PROJ"           # Jira 项目 Key，如 'PROJ', 'MYPROJECT'
JIRA_SINCE_DAYS="-30d"            # 同步最近 N 天的数据，如 '-7d', '-30d'

# --- Confluence 配置 ---
CONFLUENCE_SPACE_KEY=""           # Confluence 空间 Key，如 'TEAM', 'DOC'（留空则不同步 Confluence）
CONFLUENCE_SINCE_DAYS="30"        # 同步最近 N 天的页面

# --- Dify 配置 ---
DIFY_API_KEY="粘贴你的Dify API Key"
DIFY_API_URL="https://api.dify.ai/v1"  # 云版本用这个，自部署的改成你的地址
DIFY_DATASET_ID="粘贴你的目标知识库ID"

# --- 数据库配置 ---
# 默认使用 SQLite（留空），数据库文件将自动创建在项目目录中
# 如果需要使用 PostgreSQL 或 MySQL，请取消注释并填写连接字符串
DATABASE_URL=
# 示例 PostgreSQL: DATABASE_URL="postgresql://user:password@host:port/dbname"
# 示例 MySQL: DATABASE_URL="mysql+mysqlconnector://user:password@host:port/dbname"


# --- API 服务器配置 ---
API_HOST="0.0.0.0"                # API 服务器监听地址（0.0.0.0 表示所有网卡）
API_PORT="5000"                    # API 服务器端口
API_DEBUG="false"                  # 是否开启 Debug 模式

# --- 定时任务配置 ---
SCHEDULE_HOUR="4"                  # 定时任务执行的小时（0-23）
SCHEDULE_MINUTE="0"                # 定时任务执行的分钟（0-59）
SCHEDULE_TIMEZONE="Asia/Shanghai"  # 时区设置
